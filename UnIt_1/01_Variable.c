#include <stdio.h>

int main()
{
    int i = 10; // declRE nd intialise variable
    int j = i;
    // %d is called format specifier

    // % d is for int and %f for float and %c is for char

    printf("the value of i =%d\n the value of j= %d\n",i,j);
    return 0;
}